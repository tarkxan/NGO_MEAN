export class IDonationDetail {
  // type_name: string;
  // u_name: string;
  // donation_date: string;
  // amount: number;
  // monthly_recurrence: boolean;

  donation_type_id: string;
  user_id: string;
  donation_date: string;
  amount: number;
  monthly_recurrence: boolean;
}

